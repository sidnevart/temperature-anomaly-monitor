[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
# Подавляем шум от deprecated-вызовов в сторонних библиотеках (joblib/numpy),
# чтобы CI-лог читался. Свои предупреждения видно не будет — если важны, уберите.
filterwarnings =
    ignore::DeprecationWarning